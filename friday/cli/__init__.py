"""
F.R.I.D.A.Y. Developer CLI — Production terminal interface.

Uses the exact same execution pipeline as the Desktop UI:
    CLI → ConversationManager → FridayAgent → Kernel → Planner → Tools → Providers
"""
